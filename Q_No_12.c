#include <stdio.h>

int main() {
    char ch;

    printf("Enter any Character ? ");
    scanf("%c", &ch);

    printf("ASCII Value of %c character is : %d\n", ch, ch);

    return 0;
}

